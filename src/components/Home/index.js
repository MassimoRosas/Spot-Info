import React from 'react';

import './home.scss';

const Home = () => (
  <main className="home">
    <h1 className="home-title">Spot-Info</h1>
    <p className="home-description">A simple React app which allows you to track your Spotify data !</p>
  </main>
);

export default Home;
